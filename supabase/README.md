# BauBook Supabase

Schema e seed iniziali per BauBook! Venezia-Mestre.

## Ordine Dashboard SQL Editor

1. `migrations/0001_initial_schema.sql`
2. `seeds/venezia_mestre_demo.sql` opzionale

## Check locale

```powershell
cd C:\baubook
.\baubook.ps1 -Mode supabase-doctor
```

## CLI

La CLI Supabase verra' configurata dopo la creazione del progetto hosted `baubook-beta`.
